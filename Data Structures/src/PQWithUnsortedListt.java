// allows arbitrary element insertion and allows the removal of the element with highest priority
// When an element is added to the priority queue, the user designates its priority by providing an associated key.
// in the remove method, the element with the mininmal key will be deleted first
// pqs are related to sorting and heaps.

import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;
/*
public class PQWithUnsortedListt<K, V> extends AbstractPriorityQueue<K, V>  {
    private PositionalList<Entry<K, V>> list = new PositionalList<>();

    public PQWithUnsortedListt(){super();}

    public PQWithUnsortedListt(Comparator<K> comp){
        super(comp);
    }








    private Position<Entry<K, V>> findMin(){
        Position<Entry<K, V>> small = list.first();

        for(int i = 0; i < list.size(); i++){
            list.

        }
    }
}
*/